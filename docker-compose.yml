version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:5001"
    environment
      - FLASK_ENV=development
    volumes:
      - ./templates:/app/templates
      - ./static:/app/static
      - ./app.py:/app/app.py
